# Scrape config for the DRTC backend. Brought up by the docker-compose
# `observe` profile alongside Grafana.
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: drtc-backend
    static_configs:
      - targets: ["backend:8000"]
    metrics_path: /metrics
